
  # High-fidelity UI Design

  This is a code bundle for High-fidelity UI Design. The original project is available at https://www.figma.com/design/loV9le3Sul99dS4aoHrHoK/High-fidelity-UI-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  